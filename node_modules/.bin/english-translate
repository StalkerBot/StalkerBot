#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../american-british-english-translator/bin/english-translate" "$@"
  ret=$?
else 
  node  "$basedir/../american-british-english-translator/bin/english-translate" "$@"
  ret=$?
fi
exit $ret
